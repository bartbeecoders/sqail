Add a feature to visualize the database schema as a diagram.
Include tables, columns, primary keys, foreign keys, and relationships.
Show the diagram in a new tab.
Allow to zoom and pan the diagram.
ALlow for the user to reposition tables.
Allow color coding for tables, columns, primary keys, foreign keys, and relationships.
Allow to save the diagram as an image, pdf, or drawio file.
Allow for the user to add boxes, texts and lines.

create a new git branch for this feature.