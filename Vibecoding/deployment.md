Update the version nr (0.3.4)
Include the changelog in the release. (about page)
Check the build for warings or error and correct them.
commit, tag and push to github





update the github build pipleline to also include a release build for the db service (windows only for now)
Add a download link to the portal so users can directly download the db service.